export type VisionFrame = {
  // Placeholder for MediaPipe landmarks/frame data.
  // Define once MediaPipe integration is chosen.
  timestampMs: number;
};

export type IntensityScore = {
  value: number; // 0-100
};
